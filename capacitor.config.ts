
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.quantumnest.app',
  appName: 'Quantum Nest',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
